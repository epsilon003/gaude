# --- Core RAG pipeline ---
langchain-text-splitters>=0.3.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
torchvision>=0.19.0

# --- LLM generation (Gemini + OpenRouter, both OpenAI-compatible) ---
openai>=1.40.0

# --- App / infra ---
# The Streamlit UI was replaced by the Next.js frontend + FastAPI backend;
# uvicorn is what actually serves api.main:app (see README setup).
python-dotenv>=1.0.1
fastapi
uvicorn[standard]>=0.30.0

# --- CLI ingestion / utils ---
tqdm>=4.66.0

rank-bm25>=0.2.2
tree-sitter>=0.21.0
tree-sitter-language-pack>=0.2.0