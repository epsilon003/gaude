# URL of the FastAPI backend (see ../api/main.py). Defaults to
# http://localhost:8000 if unset — only needed if you're running the
# backend on a different host/port, or pointing at a deployed instance.
NEXT_PUBLIC_API_BASE="http://localhost:8000"
